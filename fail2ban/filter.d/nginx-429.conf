[Definition]
# Triggers when a bad actor triggers 429 errors.
failregex = (?m)^<HOST> -[^"]*"(?:GET|HEAD|POST|PUT|DELETE|CONNECT|OPTIONS|TRACE|PATCH) \S+ HTTP[^"]+" 429 \d+ "[^"]+" "[^"]+".*$
ignoreregex =
